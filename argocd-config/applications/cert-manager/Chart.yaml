# Chart wrapper para desplegar cert-manager con values desde este repo.
# La dependencia se resuelve desde el repositorio Helm de Jetstack (registrar en Argo CD si aplica).
apiVersion: v2
name: cert-manager
version: "0.1.0"
dependencies:
  - name: cert-manager
    version: "v1.19.4"
    repository: "https://charts.jetstack.io"
