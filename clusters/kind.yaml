# Configuración del cluster Kind (ejemplo)
# Documentación: https://kind.sigs.k8s.io/docs/user/configuration/

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

name: kind

# Opciones (descomenta según necesites):
# networking:
#   apiServerAddress: "127.0.0.1"
#   apiServerPort: 6443
nodes:
  - role: control-plane
  - role: worker
  # - role: worker
